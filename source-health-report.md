# 漫画源健康检查报告

检查时间：2026-08-31（Asia/Shanghai）

## 结果

- 保留源：38
- 公共站点或 API 验证通过：29
- 受地区、Cloudflare 或反爬限制但仍有有效服务证据：6
- 用户自建服务器源（仅做语法和配置检查）：3
- 更新：MangaDex 1.2.0、カドコミ 1.0.2
- 删除：漫画1234（mh1234）

## 更新详情

### MangaDex

从 1.1.1 更新到官方 1.2.0。`/manga?limit=1` API 返回有效集合数据。

### カドコミ

先更新到官方 1.0.1，再修复为 1.0.2：

- 将 BookWalker App 回退版本更新为 1.6.4。
- 修复 iTunes Lookup 返回值的版本字段解析。
- 匿名令牌接口返回 200，并成功加载首页 API（21 条新到作品）。

## 删除详情

### 漫画1234

默认域名 `b.amh1234.com` 已变成域名出售页面，不再包含源所需的 `imgBox`、`list-comic` 等漫画节点。尝试 `mh1234.com`、`mh1234.net`、`mh1234.cc` 的常见主机形式均连接失败，旧资源主机 `gmh1234.wszwhg.net` 返回 404，因此从仓库和索引移除。

## 未删除的受限源

18漫画、爱看漫、嗨皮漫画、香蕉漫画、优酷漫画和 MYCOMIC 在当前出口遇到 403、连接重置或 Cloudflare 验证。这些结果可能由地区或反爬策略造成，且对应域名仍能完成 DNS/TLS 或有官方限制说明，因此不按失效源删除。

Kavita、Komga 和 LANraragi 依赖用户自己的服务器地址，公共巡检无法判断实例可用性；本次只验证源文件语法、索引和更新地址。

## 验证项

- `index.json` 中每个文件均存在且 key 唯一。
- 38 个 JavaScript 文件均通过 `node --check`。
- `index.json` 版本与源文件版本一致。
- 所有源的顶层更新 URL 均指向本仓库 jsDelivr 地址。
- 与 Venera 官方最新 `index.json` 对比后，没有遗漏的更高版本。
